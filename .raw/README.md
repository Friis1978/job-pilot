# .raw — ingest inbox

Drop unorganized notes, research, or exported docs here, then run
`/wiki-ingest <path>` to compile them into wiki/.

This folder is gitignored and must not be scanned during normal work
(it invalidates prompt caching). It is transient input, not a source of truth.
